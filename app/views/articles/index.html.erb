<div class="navbar sticky px-3 bg-secondary-subtle border-bottom border-secondary-subtle">
  <h3 class="text-secondary fw-light">Wikinimous</h3>
  <h4><%= link_to 'New article', new_article_path %></h4>
</div>
<div class="container w-50 mt-5">
<ul style="list-style: none;" class="">
  <li>
    <% @articles.each do |article| %>
    <div>
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p class="overflow-hidden" %><%= article.content %></p>
    </div>
    <% end %>
  </li>
</ul>
</div>
